#!/usr/bin/env bash
# UserPromptSubmit hook: suggest vault search when prompt warrants it
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun run "${SCRIPT_DIR}/ts/vault-prehook.ts"
